# GlowAroma — журнал проекта

## Статус

Брендбук готов. Черновик сайта собран в Claude Design (проект `GlowAroma Catalog Mockups`,
файл `GlowAroma Сайт.dc.html`) — структура и тексты годные, визуал дорабатывается здесь.
Репозиторий: https://github.com/JIupaza/glowaroma (публичный, под GitHub Pages).

## Палитра — решение принято

Логотип реальный (тёплый оранжево-красный, росток вместо «O») **побеждает** фиолетовую палитру
брендбука. Сайт ведём в тёплой гамме, брендбук в этой части считаем устаревшим.

| Роль | Цвет |
|---|---|
| Фон основной | `#180F0D` |
| Фон секции / карточки | `#150C0B` / `#231613` |
| Акцент огонь | `#FF4D26` → `#FF9838` (градиент) |
| Глубокий красный | `#C6262C` |
| Текст | `#F7F0EC` · вторичный `#B3A39C` · приглушённый `#8C7A72` |

Шрифты: Manrope (заголовки, 700/800) + Inter (текст). Файлы в `fonts/`.

## Ассеты

**Реальные фото** (`assets/img/`) — из студийной съёмки `ИНФОГРАФИКА_\Масло 8шт кр_фото`,
автообрезка по содержимому через `prep_photos.py`:
`set8-box-front`, `set8-open`, `set8-open-angle`, `set8-alt-a/b` — набор 8×10 (арт. 189430894).
`humidifier`, `filters` — вырезаны из чистых участков `Диффузор инф-ка\Frame 41.png`.

**AI-сцены** (`assets/gen/`) — KIE.ai, `nano-banana-2`:
- `hero-atmosphere` — абстрактный фон героя, лучи справа, пустота слева под заголовок.
- `scene-kitchen` / `scene-evening` / `scene-guests` / `scene-gift` — четыре сценария дома.
  v1 забракована: нейросеть рисовала выдуманный круглый диффузор и несуществующую подарочную
  коробку. v2 генерится через `image_input` с реальным прибором и реальной коробкой.

Генерация: `bash kie_run.sh nano-banana-2 kie_inputs/<name>.json assets/gen/<out>.png`
(раннер из skill site-zavod падал на поллинге — `fetch failed`, переписан на curl).
Аплоад своего фото: `py kie_upload.py <path>` → выдаёт URL для `image_input`.
Сжатие: `py compress_gen.py`.

**Потрачено кредитов:** 60 (v1: hero + 4 сцены) + 48 (v2: 4 сцены) = 108 ≈ $1,8.

## Чего не хватает

Нет чистых товарных фото по 4 позициям: второй набор 8×10 (гардения-цитрус-сандал, арт. 255143956),
парфюмированный 6×10 (861882240), цитрус-хлопок (866952851), банный 6×10 (189434539).
Локально по ним только WB-инфографика с запрещёнными формулировками («100% ESSENTIAL OIL SET»)
и водяным знаком китайского поставщика — использовать нельзя.
Варианты: снять самим, скачать с карточек WB через `claude-in-chrome`, или чистить через image-edit.

## Запреты в текстах (жёстко)

Не писать: «100% эфирное масло», «100% органика», «лечит / снимает стресс / улучшает сон»,
«ароматерапия» в медицинском смысле, «безопасно для детей», «от комаров», «лить на камни в бане»,
«ХИТ», «ТОП-1», обещания стойкости в часах без указания дозировки.

## TODO

- [ ] Вживить v2-сцены и реальные фото в вёрстку
- [ ] Собрать сайт локально: `index.html` + `assets/style.css` (чистый HTML/CSS, без фреймворков)
- [ ] Проверить 2560 / 1440 / 768 / 375, отсутствие горизонтального скролла
- [ ] Закрыть дыру с фото по 4 товарам
- [ ] GitHub Pages + og-теги с абсолютными ссылками
